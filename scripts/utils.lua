--[[
    utils.lua
    Responsabilidade: Funções genéricas de auxílio (string, math, table).
--]]

local utils = {}

function utils.split_string(inputstr, sep)
    -- TODO: Quebrar string em array
    return {}
end

function utils.file_exists(name)
    -- TODO: Checar se um arquivo local existe
    return false
end

function utils.parse_json(json_str)
    -- TODO: Implementação simples ou stub para decoder de JSON
    return {}
end

return utils
